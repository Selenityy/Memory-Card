// import { useState } from "react";
import Pokemon from "./Pokemon";

  // fetch the API
  // create n cards from API
  // on card click it will
  // 1) randomize the cards & display again
  // 2) updated if card is clicked
  // 3) check if win (different component?)
  // 4) update the scores (different component?)
